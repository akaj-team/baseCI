name: CI

on:
  push:
    branches:
      - 'test-github-action'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a multi-line script
        run: |
          ./gradlew assembleDebug,
          ./gradlew appDistributionUpload.
